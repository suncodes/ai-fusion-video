# 测试记录

## 校验范围

- 视频输入模式归一化。
- Ark `content` 互斥组合。
- 尾帧依赖首帧。
- Seedance 2.0 duration 边界。
- 参考图生成关键帧后再生成视频的两阶段流程。
- 历史任务和重复重试行为。

## 测试矩阵

| 场景 | 预期结果 |
| --- | --- |
| 首帧 + 尾帧 | 发送 `first_frame + last_frame`，不发送任何参考媒体 |
| 首帧 + 参考图 | 进入首帧模式，参考图被裁剪并记录 warning |
| 首尾帧 + 参考视频/音频 | 进入首尾帧模式，参考媒体被裁剪并记录 warning |
| 只有尾帧 | 本地返回 `VIDEO_LAST_FRAME_REQUIRES_FIRST_FRAME` |
| 无帧 + 参考图/视频/音频 | 进入多模态参考模式 |
| duration=3 | 本地拒绝，不调用 Ark |
| duration=4 或 15 | 允许提交 |
| duration=16 | 本地拒绝，不调用 Ark |
| 参考图生成首尾帧成功 | 保存帧后只提交一次视频请求 |
| 参考图生成关键帧失败 | 不提交视频，返回关键帧阶段错误 |

## 编译结果

- 命令：`mvn "-DskipTests" compile`
- 环境：`JAVA_HOME=D:/software/install/Java/jdk-23.0.1`，工程按 `--release 21` 编译。
- 结果：通过，`BUILD SUCCESS`。

## 定向测试结果

- 命令：`mvn "-Dtest=GenerationModelCapabilityServiceTests,GetGenerationModelCapabilitiesToolExecutorTests,VolcengineVideoStrategyTests" test`
- 结果：通过，`Tests run: 23, Failures: 0, Errors: 0, Skipped: 0`。
- 覆盖点：
  - Seedance 2.0 参考媒体无首尾帧时允许；
  - 首尾帧与参考媒体混用时拒绝；
  - 只有尾帧没有首帧时拒绝；
  - Seedance 2.0 duration 4-15 秒边界；
  - Ark content 构造时首尾帧模式不会携带 reference media；
  - 无首尾帧时 reference media 正常构造。

## checkstyle 结果

- 命令：`mvn "-DskipTests" checkstyle:check`
- 结果：未通过。
- 原因：当前工程使用默认 `sun_checks.xml`，全仓已有 `9494` 个 checkstyle 违规，主要是既有 Javadoc、80 字符行宽、final 参数、魔术数字等规则问题；本次实现未尝试在同一需求中修复全仓历史 checkstyle 债务。

## 未执行项

- Ark 真实联调：当前未发送真实平台请求，避免产生外部任务和费用。
- 全量回归测试：未执行；本轮执行了与 Seedance 输入互斥和 duration 相关的定向测试。

## 风险

- 产品尚未最终确认低于 4 秒镜头的处理策略。
- 参考图被裁剪后，角色一致性依赖关键帧质量和视频 prompt；需要通过关键帧阶段的图片模型和提示词保证。
